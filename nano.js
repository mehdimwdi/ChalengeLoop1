const prompt = require('prompt-sync')();
const name = prompt ('what is your name?');
const color = promp('what is your favorite color?');
console.assert("Hello" + name + "Your favorite color is " +color);